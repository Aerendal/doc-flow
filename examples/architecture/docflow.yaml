docs_root: examples/architecture
ignore_patterns: []
cache:
  enabled: false
  dir: .docflow/cache
  max_size_mb: 10
  max_age_days: 7
log:
  level: info
  format: text
db:
  path: .docflow/docflow.db
dependency:
  promote_context_for: []
section_aliases:
  Przegląd: ["Overview"]
  Decyzje architektoniczne: ["Architecture decisions"]
  Komponenty: ["Components"]
  Ryzyka: ["Risks"]
